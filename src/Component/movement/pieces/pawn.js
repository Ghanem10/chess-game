/**
 * pawn move forward, that means we have to allocate its position
 * than, we can make/apply moves based on the current pistion.
 */

export default function MovePawn(postion) {
    console.log(postion)
}