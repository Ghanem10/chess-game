
//TODO, doesn't seem to work using jest configuration - ECMAScript Modules.

import app from '../server';
// import loginProdiver from '../routes/loginProviders';
import request from 'supertest';
import assert from 'assert';
// import passport from 'passport';

// jest.mock(passport);

describe("", () => {
    it("true", () => {

        //
        expect(true).toBe(true);
    })
})